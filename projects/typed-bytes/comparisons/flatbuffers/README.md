# FlatBuffers Comparison Project

*A minimal FlatBuffers project to facilitate comparison with typed-bytes.*

## Build and Run

Prerequisites:
- nodejs
- flatc (e.g. `sudo apt install flatbuffers-compiler`)

```sh
npm install
npm run build
node index.js
```
